/**
 * Mercatorio API
 * API to interact with the game of Mercatorio
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BusinessAccount from './BusinessAccount';
import BusinessBuildings from './BusinessBuildings';

/**
 * The Business model module.
 * @module model/Business
 * @version 1.0.0
 */
class Business {
    /**
     * Constructs a new <code>Business</code>.
     * @alias module:model/Business
     */
    constructor() { 
        
        Business.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Business</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Business} obj Optional instance to populate.
     * @return {module:model/Business} The populated <code>Business</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Business();

            if (data.hasOwnProperty('account')) {
                obj['account'] = BusinessAccount.constructFromObject(data['account']);
            }
            if (data.hasOwnProperty('buildings')) {
                obj['buildings'] = ApiClient.convertToType(data['buildings'], [BusinessBuildings]);
            }
            if (data.hasOwnProperty('building_ids')) {
                obj['building_ids'] = ApiClient.convertToType(data['building_ids'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('reference')) {
                obj['reference'] = ApiClient.convertToType(data['reference'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('portrait')) {
                obj['portrait'] = ApiClient.convertToType(data['portrait'], 'String');
            }
            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('owner_id')) {
                obj['owner_id'] = ApiClient.convertToType(data['owner_id'], 'String');
            }
            if (data.hasOwnProperty('transport_ids')) {
                obj['transport_ids'] = ApiClient.convertToType(data['transport_ids'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Business</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Business</code>.
     */
    static validateJSON(data) {
        // validate the optional field `account`
        if (data['account']) { // data not null
          BusinessAccount.validateJSON(data['account']);
        }
        if (data['buildings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['buildings'])) {
                throw new Error("Expected the field `buildings` to be an array in the JSON data but got " + data['buildings']);
            }
            // validate the optional field `buildings` (array)
            for (const item of data['buildings']) {
                BusinessBuildings.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['building_ids'])) {
            throw new Error("Expected the field `building_ids` to be an array in the JSON data but got " + data['building_ids']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['reference'] && !(typeof data['reference'] === 'string' || data['reference'] instanceof String)) {
            throw new Error("Expected the field `reference` to be a primitive type in the JSON string but got " + data['reference']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['portrait'] && !(typeof data['portrait'] === 'string' || data['portrait'] instanceof String)) {
            throw new Error("Expected the field `portrait` to be a primitive type in the JSON string but got " + data['portrait']);
        }
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is a string
        if (data['owner_id'] && !(typeof data['owner_id'] === 'string' || data['owner_id'] instanceof String)) {
            throw new Error("Expected the field `owner_id` to be a primitive type in the JSON string but got " + data['owner_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['transport_ids'])) {
            throw new Error("Expected the field `transport_ids` to be an array in the JSON data but got " + data['transport_ids']);
        }

        return true;
    }


}



/**
 * @member {module:model/BusinessAccount} account
 */
Business.prototype['account'] = undefined;

/**
 * @member {Array.<module:model/BusinessBuildings>} buildings
 */
Business.prototype['buildings'] = undefined;

/**
 * @member {Array.<String>} building_ids
 */
Business.prototype['building_ids'] = undefined;

/**
 * @member {String} id
 */
Business.prototype['id'] = undefined;

/**
 * @member {String} reference
 */
Business.prototype['reference'] = undefined;

/**
 * @member {String} name
 */
Business.prototype['name'] = undefined;

/**
 * @member {String} portrait
 */
Business.prototype['portrait'] = undefined;

/**
 * @member {String} account_id
 */
Business.prototype['account_id'] = undefined;

/**
 * @member {String} owner_id
 */
Business.prototype['owner_id'] = undefined;

/**
 * @member {Array.<String>} transport_ids
 */
Business.prototype['transport_ids'] = undefined;






export default Business;

