/**
 * Mercatorio API
 * API to interact with the game of Mercatorio
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BuildingPreviousOperation from './BuildingPreviousOperation';
import Inventory from './Inventory';

/**
 * The BuildingProducer model module.
 * @module model/BuildingProducer
 * @version 1.0.0
 */
class BuildingProducer {
    /**
     * Constructs a new <code>BuildingProducer</code>.
     * @alias module:model/BuildingProducer
     */
    constructor() { 
        
        BuildingProducer.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BuildingProducer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BuildingProducer} obj Optional instance to populate.
     * @return {module:model/BuildingProducer} The populated <code>BuildingProducer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BuildingProducer();

            if (data.hasOwnProperty('reference')) {
                obj['reference'] = ApiClient.convertToType(data['reference'], 'String');
            }
            if (data.hasOwnProperty('inventory')) {
                obj['inventory'] = Inventory.constructFromObject(data['inventory']);
            }
            if (data.hasOwnProperty('target')) {
                obj['target'] = ApiClient.convertToType(data['target'], 'String');
            }
            if (data.hasOwnProperty('limited')) {
                obj['limited'] = ApiClient.convertToType(data['limited'], 'Boolean');
            }
            if (data.hasOwnProperty('settings')) {
                obj['settings'] = ApiClient.convertToType(data['settings'], Object);
            }
            if (data.hasOwnProperty('recipe')) {
                obj['recipe'] = ApiClient.convertToType(data['recipe'], 'String');
            }
            if (data.hasOwnProperty('previous_operation')) {
                obj['previous_operation'] = BuildingPreviousOperation.constructFromObject(data['previous_operation']);
            }
            if (data.hasOwnProperty('manager')) {
                obj['manager'] = ApiClient.convertToType(data['manager'], 'String');
            }
            if (data.hasOwnProperty('provider_id')) {
                obj['provider_id'] = ApiClient.convertToType(data['provider_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BuildingProducer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BuildingProducer</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['reference'] && !(typeof data['reference'] === 'string' || data['reference'] instanceof String)) {
            throw new Error("Expected the field `reference` to be a primitive type in the JSON string but got " + data['reference']);
        }
        // validate the optional field `inventory`
        if (data['inventory']) { // data not null
          Inventory.validateJSON(data['inventory']);
        }
        // ensure the json data is a string
        if (data['target'] && !(typeof data['target'] === 'string' || data['target'] instanceof String)) {
            throw new Error("Expected the field `target` to be a primitive type in the JSON string but got " + data['target']);
        }
        // ensure the json data is a string
        if (data['recipe'] && !(typeof data['recipe'] === 'string' || data['recipe'] instanceof String)) {
            throw new Error("Expected the field `recipe` to be a primitive type in the JSON string but got " + data['recipe']);
        }
        // validate the optional field `previous_operation`
        if (data['previous_operation']) { // data not null
          BuildingPreviousOperation.validateJSON(data['previous_operation']);
        }
        // ensure the json data is a string
        if (data['manager'] && !(typeof data['manager'] === 'string' || data['manager'] instanceof String)) {
            throw new Error("Expected the field `manager` to be a primitive type in the JSON string but got " + data['manager']);
        }
        // ensure the json data is a string
        if (data['provider_id'] && !(typeof data['provider_id'] === 'string' || data['provider_id'] instanceof String)) {
            throw new Error("Expected the field `provider_id` to be a primitive type in the JSON string but got " + data['provider_id']);
        }

        return true;
    }


}



/**
 * @member {String} reference
 */
BuildingProducer.prototype['reference'] = undefined;

/**
 * @member {module:model/Inventory} inventory
 */
BuildingProducer.prototype['inventory'] = undefined;

/**
 * @member {String} target
 */
BuildingProducer.prototype['target'] = undefined;

/**
 * @member {Boolean} limited
 */
BuildingProducer.prototype['limited'] = undefined;

/**
 * @member {Object} settings
 */
BuildingProducer.prototype['settings'] = undefined;

/**
 * @member {String} recipe
 */
BuildingProducer.prototype['recipe'] = undefined;

/**
 * @member {module:model/BuildingPreviousOperation} previous_operation
 */
BuildingProducer.prototype['previous_operation'] = undefined;

/**
 * @member {String} manager
 */
BuildingProducer.prototype['manager'] = undefined;

/**
 * @member {String} provider_id
 */
BuildingProducer.prototype['provider_id'] = undefined;






export default BuildingProducer;

