/**
 * Mercatorio API
 * API to interact with the game of Mercatorio
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Household from './Household';
import PlayerActivity from './PlayerActivity';
import PlayerSettings from './PlayerSettings';

/**
 * The Player model module.
 * @module model/Player
 * @version 1.0.0
 */
class Player {
    /**
     * Constructs a new <code>Player</code>.
     * @alias module:model/Player
     * @param username {String} 
     * @param household {module:model/Household} 
     * @param active {Boolean} 
     */
    constructor(username, household, active) { 
        
        Player.initialize(this, username, household, active);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, household, active) { 
        obj['username'] = username;
        obj['household'] = household;
        obj['active'] = active;
    }

    /**
     * Constructs a <code>Player</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Player} obj Optional instance to populate.
     * @return {module:model/Player} The populated <code>Player</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Player();

            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('household')) {
                obj['household'] = Household.constructFromObject(data['household']);
            }
            if (data.hasOwnProperty('discord_id')) {
                obj['discord_id'] = ApiClient.convertToType(data['discord_id'], 'String');
            }
            if (data.hasOwnProperty('settings')) {
                obj['settings'] = PlayerSettings.constructFromObject(data['settings']);
            }
            if (data.hasOwnProperty('bonus_points')) {
                obj['bonus_points'] = ApiClient.convertToType(data['bonus_points'], 'Number');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('activity')) {
                obj['activity'] = PlayerActivity.constructFromObject(data['activity']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Player</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Player</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Player.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // validate the optional field `household`
        if (data['household']) { // data not null
          Household.validateJSON(data['household']);
        }
        // ensure the json data is a string
        if (data['discord_id'] && !(typeof data['discord_id'] === 'string' || data['discord_id'] instanceof String)) {
            throw new Error("Expected the field `discord_id` to be a primitive type in the JSON string but got " + data['discord_id']);
        }
        // validate the optional field `settings`
        if (data['settings']) { // data not null
          PlayerSettings.validateJSON(data['settings']);
        }
        // validate the optional field `activity`
        if (data['activity']) { // data not null
          PlayerActivity.validateJSON(data['activity']);
        }

        return true;
    }


}

Player.RequiredProperties = ["username", "household", "active"];

/**
 * @member {String} username
 */
Player.prototype['username'] = undefined;

/**
 * @member {module:model/Household} household
 */
Player.prototype['household'] = undefined;

/**
 * @member {String} discord_id
 */
Player.prototype['discord_id'] = undefined;

/**
 * @member {module:model/PlayerSettings} settings
 */
Player.prototype['settings'] = undefined;

/**
 * @member {Number} bonus_points
 */
Player.prototype['bonus_points'] = undefined;

/**
 * @member {Boolean} active
 */
Player.prototype['active'] = undefined;

/**
 * @member {module:model/PlayerActivity} activity
 */
Player.prototype['activity'] = undefined;






export default Player;

