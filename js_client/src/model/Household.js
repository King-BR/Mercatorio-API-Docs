/**
 * Mercatorio API
 * API to interact with the game of Mercatorio
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HouseholdCaps from './HouseholdCaps';
import HouseholdPrestigeImpactsInner from './HouseholdPrestigeImpactsInner';
import HouseholdSustenance from './HouseholdSustenance';
import Specialization from './Specialization';
import Worker from './Worker';

/**
 * The Household model module.
 * @module model/Household
 * @version 1.0.0
 */
class Household {
    /**
     * Constructs a new <code>Household</code>.
     * @alias module:model/Household
     */
    constructor() { 
        
        Household.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Household</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Household} obj Optional instance to populate.
     * @return {module:model/Household} The populated <code>Household</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Household();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('town_id')) {
                obj['town_id'] = ApiClient.convertToType(data['town_id'], 'String');
            }
            if (data.hasOwnProperty('portrait')) {
                obj['portrait'] = ApiClient.convertToType(data['portrait'], 'String');
            }
            if (data.hasOwnProperty('gender')) {
                obj['gender'] = ApiClient.convertToType(data['gender'], 'String');
            }
            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('business_ids')) {
                obj['business_ids'] = ApiClient.convertToType(data['business_ids'], ['String']);
            }
            if (data.hasOwnProperty('prestige')) {
                obj['prestige'] = ApiClient.convertToType(data['prestige'], 'String');
            }
            if (data.hasOwnProperty('prestige_impacts')) {
                obj['prestige_impacts'] = ApiClient.convertToType(data['prestige_impacts'], [HouseholdPrestigeImpactsInner]);
            }
            if (data.hasOwnProperty('legacy')) {
                obj['legacy'] = ApiClient.convertToType(data['legacy'], 'String');
            }
            if (data.hasOwnProperty('workers')) {
                obj['workers'] = ApiClient.convertToType(data['workers'], [Worker]);
            }
            if (data.hasOwnProperty('specialization')) {
                obj['specialization'] = Specialization.constructFromObject(data['specialization']);
            }
            if (data.hasOwnProperty('operations')) {
                obj['operations'] = ApiClient.convertToType(data['operations'], ['String']);
            }
            if (data.hasOwnProperty('caps')) {
                obj['caps'] = HouseholdCaps.constructFromObject(data['caps']);
            }
            if (data.hasOwnProperty('sustenance')) {
                obj['sustenance'] = HouseholdSustenance.constructFromObject(data['sustenance']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Household</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Household</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['town_id'] && !(typeof data['town_id'] === 'string' || data['town_id'] instanceof String)) {
            throw new Error("Expected the field `town_id` to be a primitive type in the JSON string but got " + data['town_id']);
        }
        // ensure the json data is a string
        if (data['portrait'] && !(typeof data['portrait'] === 'string' || data['portrait'] instanceof String)) {
            throw new Error("Expected the field `portrait` to be a primitive type in the JSON string but got " + data['portrait']);
        }
        // ensure the json data is a string
        if (data['gender'] && !(typeof data['gender'] === 'string' || data['gender'] instanceof String)) {
            throw new Error("Expected the field `gender` to be a primitive type in the JSON string but got " + data['gender']);
        }
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['business_ids'])) {
            throw new Error("Expected the field `business_ids` to be an array in the JSON data but got " + data['business_ids']);
        }
        // ensure the json data is a string
        if (data['prestige'] && !(typeof data['prestige'] === 'string' || data['prestige'] instanceof String)) {
            throw new Error("Expected the field `prestige` to be a primitive type in the JSON string but got " + data['prestige']);
        }
        if (data['prestige_impacts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['prestige_impacts'])) {
                throw new Error("Expected the field `prestige_impacts` to be an array in the JSON data but got " + data['prestige_impacts']);
            }
            // validate the optional field `prestige_impacts` (array)
            for (const item of data['prestige_impacts']) {
                HouseholdPrestigeImpactsInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['legacy'] && !(typeof data['legacy'] === 'string' || data['legacy'] instanceof String)) {
            throw new Error("Expected the field `legacy` to be a primitive type in the JSON string but got " + data['legacy']);
        }
        if (data['workers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['workers'])) {
                throw new Error("Expected the field `workers` to be an array in the JSON data but got " + data['workers']);
            }
            // validate the optional field `workers` (array)
            for (const item of data['workers']) {
                Worker.validateJSON(item);
            };
        }
        // validate the optional field `specialization`
        if (data['specialization']) { // data not null
          Specialization.validateJSON(data['specialization']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['operations'])) {
            throw new Error("Expected the field `operations` to be an array in the JSON data but got " + data['operations']);
        }
        // validate the optional field `caps`
        if (data['caps']) { // data not null
          HouseholdCaps.validateJSON(data['caps']);
        }
        // validate the optional field `sustenance`
        if (data['sustenance']) { // data not null
          HouseholdSustenance.validateJSON(data['sustenance']);
        }

        return true;
    }


}



/**
 * @member {String} id
 */
Household.prototype['id'] = undefined;

/**
 * @member {String} name
 */
Household.prototype['name'] = undefined;

/**
 * @member {String} town_id
 */
Household.prototype['town_id'] = undefined;

/**
 * @member {String} portrait
 */
Household.prototype['portrait'] = undefined;

/**
 * @member {String} gender
 */
Household.prototype['gender'] = undefined;

/**
 * @member {String} account_id
 */
Household.prototype['account_id'] = undefined;

/**
 * @member {Array.<String>} business_ids
 */
Household.prototype['business_ids'] = undefined;

/**
 * @member {String} prestige
 */
Household.prototype['prestige'] = undefined;

/**
 * @member {Array.<module:model/HouseholdPrestigeImpactsInner>} prestige_impacts
 */
Household.prototype['prestige_impacts'] = undefined;

/**
 * @member {String} legacy
 */
Household.prototype['legacy'] = undefined;

/**
 * @member {Array.<module:model/Worker>} workers
 */
Household.prototype['workers'] = undefined;

/**
 * @member {module:model/Specialization} specialization
 */
Household.prototype['specialization'] = undefined;

/**
 * @member {Array.<String>} operations
 */
Household.prototype['operations'] = undefined;

/**
 * @member {module:model/HouseholdCaps} caps
 */
Household.prototype['caps'] = undefined;

/**
 * @member {module:model/HouseholdSustenance} sustenance
 */
Household.prototype['sustenance'] = undefined;






export default Household;

