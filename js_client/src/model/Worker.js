/**
 * Mercatorio API
 * API to interact with the game of Mercatorio
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WorkerSkills from './WorkerSkills';

/**
 * The Worker model module.
 * @module model/Worker
 * @version 1.0.0
 */
class Worker {
    /**
     * Constructs a new <code>Worker</code>.
     * @alias module:model/Worker
     * @param name {String} 
     */
    constructor(name) { 
        
        Worker.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>Worker</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Worker} obj Optional instance to populate.
     * @return {module:model/Worker} The populated <code>Worker</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Worker();

            if (data.hasOwnProperty('assignment')) {
                obj['assignment'] = ApiClient.convertToType(data['assignment'], 'String');
            }
            if (data.hasOwnProperty('capacity')) {
                obj['capacity'] = ApiClient.convertToType(data['capacity'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('skills')) {
                obj['skills'] = WorkerSkills.constructFromObject(data['skills']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Worker</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Worker</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Worker.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['assignment'] && !(typeof data['assignment'] === 'string' || data['assignment'] instanceof String)) {
            throw new Error("Expected the field `assignment` to be a primitive type in the JSON string but got " + data['assignment']);
        }
        // ensure the json data is a string
        if (data['capacity'] && !(typeof data['capacity'] === 'string' || data['capacity'] instanceof String)) {
            throw new Error("Expected the field `capacity` to be a primitive type in the JSON string but got " + data['capacity']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `skills`
        if (data['skills']) { // data not null
          WorkerSkills.validateJSON(data['skills']);
        }

        return true;
    }


}

Worker.RequiredProperties = ["name"];

/**
 * @member {String} assignment
 */
Worker.prototype['assignment'] = undefined;

/**
 * @member {String} capacity
 */
Worker.prototype['capacity'] = undefined;

/**
 * @member {String} name
 */
Worker.prototype['name'] = undefined;

/**
 * @member {module:model/WorkerSkills} skills
 */
Worker.prototype['skills'] = undefined;






export default Worker;

