/**
 * Mercatorio API
 * API to interact with the game of Mercatorio
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BuildingConstructionSettings from './BuildingConstructionSettings';
import BuildingLocation from './BuildingLocation';
import Inventory from './Inventory';

/**
 * The BuildingConstruction model module.
 * @module model/BuildingConstruction
 * @version 1.0.0
 */
class BuildingConstruction {
    /**
     * Constructs a new <code>BuildingConstruction</code>.
     * @alias module:model/BuildingConstruction
     */
    constructor() { 
        
        BuildingConstruction.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BuildingConstruction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BuildingConstruction} obj Optional instance to populate.
     * @return {module:model/BuildingConstruction} The populated <code>BuildingConstruction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BuildingConstruction();

            if (data.hasOwnProperty('reference')) {
                obj['reference'] = ApiClient.convertToType(data['reference'], 'String');
            }
            if (data.hasOwnProperty('inventory')) {
                obj['inventory'] = Inventory.constructFromObject(data['inventory']);
            }
            if (data.hasOwnProperty('size')) {
                obj['size'] = ApiClient.convertToType(data['size'], 'Number');
            }
            if (data.hasOwnProperty('time')) {
                obj['time'] = ApiClient.convertToType(data['time'], 'Number');
            }
            if (data.hasOwnProperty('total_time')) {
                obj['total_time'] = ApiClient.convertToType(data['total_time'], 'Number');
            }
            if (data.hasOwnProperty('progress')) {
                obj['progress'] = ApiClient.convertToType(data['progress'], 'Number');
            }
            if (data.hasOwnProperty('stage')) {
                obj['stage'] = ApiClient.convertToType(data['stage'], 'String');
            }
            if (data.hasOwnProperty('land')) {
                obj['land'] = ApiClient.convertToType(data['land'], [BuildingLocation]);
            }
            if (data.hasOwnProperty('provider_id')) {
                obj['provider_id'] = ApiClient.convertToType(data['provider_id'], 'String');
            }
            if (data.hasOwnProperty('settings')) {
                obj['settings'] = BuildingConstructionSettings.constructFromObject(data['settings']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BuildingConstruction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BuildingConstruction</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['reference'] && !(typeof data['reference'] === 'string' || data['reference'] instanceof String)) {
            throw new Error("Expected the field `reference` to be a primitive type in the JSON string but got " + data['reference']);
        }
        // validate the optional field `inventory`
        if (data['inventory']) { // data not null
          Inventory.validateJSON(data['inventory']);
        }
        // ensure the json data is a string
        if (data['stage'] && !(typeof data['stage'] === 'string' || data['stage'] instanceof String)) {
            throw new Error("Expected the field `stage` to be a primitive type in the JSON string but got " + data['stage']);
        }
        if (data['land']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['land'])) {
                throw new Error("Expected the field `land` to be an array in the JSON data but got " + data['land']);
            }
            // validate the optional field `land` (array)
            for (const item of data['land']) {
                BuildingLocation.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['provider_id'] && !(typeof data['provider_id'] === 'string' || data['provider_id'] instanceof String)) {
            throw new Error("Expected the field `provider_id` to be a primitive type in the JSON string but got " + data['provider_id']);
        }
        // validate the optional field `settings`
        if (data['settings']) { // data not null
          BuildingConstructionSettings.validateJSON(data['settings']);
        }

        return true;
    }


}



/**
 * @member {String} reference
 */
BuildingConstruction.prototype['reference'] = undefined;

/**
 * @member {module:model/Inventory} inventory
 */
BuildingConstruction.prototype['inventory'] = undefined;

/**
 * @member {Number} size
 */
BuildingConstruction.prototype['size'] = undefined;

/**
 * @member {Number} time
 */
BuildingConstruction.prototype['time'] = undefined;

/**
 * @member {Number} total_time
 */
BuildingConstruction.prototype['total_time'] = undefined;

/**
 * @member {Number} progress
 */
BuildingConstruction.prototype['progress'] = undefined;

/**
 * @member {String} stage
 */
BuildingConstruction.prototype['stage'] = undefined;

/**
 * @member {Array.<module:model/BuildingLocation>} land
 */
BuildingConstruction.prototype['land'] = undefined;

/**
 * @member {String} provider_id
 */
BuildingConstruction.prototype['provider_id'] = undefined;

/**
 * @member {module:model/BuildingConstructionSettings} settings
 */
BuildingConstruction.prototype['settings'] = undefined;






export default BuildingConstruction;

